import 'dart:math';

const kPadding = 16.0;
const kBorderRadius = 16.0;

const kFastAnimationDuration = Duration(milliseconds: 150);
const kMediumAnimationDuration = Duration(milliseconds: 250);
const kSlowAnimationDuration = Duration(milliseconds: 400);

final random = Random();
